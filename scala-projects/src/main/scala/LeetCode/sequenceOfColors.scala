package LeetCode

object sequenceOfColors {

  def main(args: Array[String]): Unit = {
    val t = io.StdIn.readInt()
    (1 to t).foreach(x => {
      val seq = io.StdIn.readLine()
      
    })
  }
}
